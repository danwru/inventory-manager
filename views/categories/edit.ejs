<% layout('layouts/boilerplate') %>

<!--

<h1>Edit Category</h1>
<form action="/categories/<%=category._id%>?_method=PUT" method="POST">
    <div>
        <label for="name">Name</label>
        <input type="text" id="name" name="category[name]" value="<%= category.name %> ">
    </div>
    <div>
        <label for="description">Description</label>
        <input type="text" id="description" name="category[description]" value="<%= category.description %> ">
    </div>
    <button>Update Category</button>
</form>
<a href="/categories/<%= category._id %> ">Back to Category</a>

-->


<div class="row">
    <h1 class="text-center">Edit Category</h1>
    <div class="col-6 offset-3">
        <form action="/categories/<%= category._id %>?_method=PUT " method="POST">
            <div class="mb-3">
                <label class="form-label" for="title">Title</label>
                <input class="form-control" type="text" id="title" name="categories[name]" value="<%= category.name%> ">
            </div>
            <div class="mb-3">
                <label class="form-label" for="description">Description</label>
                <textarea class="form-control" type="text" id="description" name="category[description]"><%= category.description %></textarea>
            </div>
            <div class="mb-3">
                <button class="btn btn-info">Update Category</button>
            </div>
        </form>
        <a href="/categories/<%= category._id %> ">Back to Categories</a>
    </div>
</div>
